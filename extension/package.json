{
  "name": "printmd-extension",
  "version": "1.0.0",
  "description": "Open GitHub markdown files in printmd for styling and PDF export",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.3",
    "vite": "^6.1.1"
  }
}
